{
  "name": "C + libxlsxwriter",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y build-essential pkg-config libxlsxwriter-dev && sudo rm -rf /var/lib/apt/lists/*",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.cpptools"]
    }
  }
}